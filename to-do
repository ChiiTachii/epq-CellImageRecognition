To do.

Modulate each function within the model. tick

Create micro analysis on specific classes (macro was confusion matrix).

Train. tick

Create the validation set. tick

Create a model utilising dropout to increase accuracy. tick

Comment everything. tick
